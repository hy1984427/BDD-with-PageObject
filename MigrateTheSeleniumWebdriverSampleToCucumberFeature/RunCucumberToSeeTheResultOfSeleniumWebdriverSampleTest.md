# Run cucumber to see the result of Selenium-WebDriver sample test

Now we need to run Selenium-WebDriver sample test under "Cucumber" folder. Use

`cd ../..`

`cucumber features/sample.feature`

to just run only one feature - features/sample.feature, and check the result.

![alt text](https://raw.githubusercontent.com/hy1984427/BDD-with-PageObject/master/images/SeleniumWebDriverSampleResult.png "Selenium-WebDriver sample Cucumber Result")
